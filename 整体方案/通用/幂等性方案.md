## 第一层

- **前端防抖**：用户点击提交按钮后，立即将其置灰或禁用，防止用户手抖连续点击。
- **网关防重**：在 API 网关层对短时间内的完全相同的请求进行拦截。

## 第二层

- **Token 令牌机制（适合表单提交/无天然唯一键）**

- **分布式锁（适合高并发写/资源争用）**

- **全局唯一 Request ID / 幂等键（适合资金交易等核心业务）**

## 第三层

- **数据库唯一索引（适合新增/插入操作）**

- **状态机 + 乐观锁（适合更新/状态流转操作）**

- **去重表 / 幂等日志表（适合通用场景/MQ消费）**