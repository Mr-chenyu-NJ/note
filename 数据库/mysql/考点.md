## 什么是不可重读

侧重于**数据内容的修改**。指的是在同一个事务内，两次读取**同一行数据**，因为其他事务执行了 UPDATE或 DELETE 操作，导致两次读取到的`值不一样`

## 什么是幻读

在同一个事务内，两次执行相同的范围查询，第二次查询到了第一次没有的“新行”，导致前后两次查询的结果集行数不一致,重点是`行数发生变化`

## 为什么说mysql解决部分幻读

在纯当前读和纯快照下 都没问题 但是混合用就会出现幻读